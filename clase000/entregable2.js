/*
function saludar() {
    return 'Hola DJ'
}

const segundaFun = (a, b) => {
    return (a + b)
}
function terceraFun(a) {
    return function () {

    }
}

segundaFun(5, 5)
saludar()
terceraFun(5)

const array = ['gato', 'perro', 'ratón', 'gallina']
array.forEach()
array.filter()

function cbFun(param) {
    return `el usuario inició el ${param}`
}

function funDos(p1, callback) {
    const respuesta = callback(p1)
    return respuesta
}

console.log(funDos('curso', cbFun));

*/
/*
const n = 100

const resultado = ((n * (n + 1)) / 2)
console.log(resultado)

setTimeout(() => {
   console.log('Primary'); 
}, 2000);

setTimeout(() => {
    console.log('Segunda Línea');
}, 2000)
console.log('Secondary');
*/

  const fs = require('fs')

// fs.writeFileSync('archivo.txt', 'Primera Línea')
 //fs.writeFileSync('archivo2.txt', 'Segunda Línea')
// fs.appendFileSync('archivo.txt', 'Tercera Línea')
// const infoArchivo = fs.readFileSync('archivo.txt', 'utf-8')

//fs.unlinkSync('archivo.txt')

// fs.readFile('archivo.txt', 'utf-8', (error, info) => {
//     if (error) {
//         console.log('error: no se encontró el archivo');
//     } else {
//         console.log(info);
//     }
// })

// const fs = require('fs')
// const date = new Date().toLocaleDateString()

// fs.writeFile('archivo.txt', date, (error,) => {
//     if (error) console.log(error)
// })

// fs.readFile('archivo.txt', 'utf-8', (error, str) => {
//     if (error) console.log(str)
// })

